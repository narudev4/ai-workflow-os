# AGENTS.md

このリポジトリで作業するAIエージェント向けの運用ルール。

## 目的

AI Workflow OS は、AIそのものを中心にした自動化ではなく、業務・案件情報を AI が扱える構造にするための設計リポジトリです。

## 基本方針

- 日本語で簡潔に作業する
- 確認済みの事実と未確認の仮説を混ぜない
- GitHub を正本、Google Sheets を表示・共有先として扱う
- クライアント合意前の不明点は `questions.csv` に置く
- 詳細設計、API設計、DB設計は要件定義フェーズに持ち込まない
- 変更理由が後から重要になるものは `decisions/` に記録する

## タスク境界

作業単位はセッションではなく成果物で区切る。

各タスクは次の形で定義する。

```text
Task:
Input:
Output:
Done:
```

例:

```text
Task: 画面一覧の作成
Input: モック、要求整理、過去決定事項
Output: screens.csv
Done:
- モック上の画面を全列挙している
- 主要な状態、権限、例外候補を見ている
- 未確定事項が questions.csv に登録されている
```

## 要件定義の境界

このリポジトリで扱う要件定義は次まで。

- 要求整理
- 画面一覧
- 機能一覧
- 工程表
- 質問表
- テストケース
- 先方合意

次は範囲外。

- DBテーブル定義
- API詳細仕様
- コンポーネント詳細設計
- 実装方針の細部

## 情報昇格ルール

- 会話中の思いつきはメモに留める
- 確定した要求だけ `requirements.md` に入れる
- 先方確認が必要なものは `questions.csv` に入れる
- 意思決定になったものは `decisions/` に入れる
- Sheets は GitHub の内容を見せるためのビューとして更新する

## 改善ループ

Hook、JSONLログ分析、AGENTS.md / Skill の自己改善は、最初の案件ループが動いてから扱う。

改善候補は即時反映しない。次を満たすときだけルール化する。

- 同じ失敗が再発している
- 原因がコンテキスト不足、手順不足、テンプレート不足のいずれかで説明できる
- AGENTS.md、Skill、テンプレート、テストのいずれかに落とすと再発防止になる

